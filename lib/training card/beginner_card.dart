import 'package:flutter/material.dart';

class BeginnerCard extends StatelessWidget {
  const BeginnerCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
